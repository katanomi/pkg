# Test case: object parameter with defined properties and extra default values
# Test Intent: Verify function filters ObjectVal to only include properties defined in Properties schema
# Key validation points:
# 1. Only properties defined in Properties should be retained
# 2. Extra undefined properties should be filtered out
# 3. Valid properties should preserve their values

name: "test-defined-properties"
type: "object"
description: "Test object parameter with defined properties and extra defaults"
properties:
  allowedKey1:
    type: "string"
  allowedKey2:
    type: "string"
default:
  allowedKey1: "value1"
  allowedKey2: "value2"
  extraKey1: "should-be-removed"
  extraKey2: "should-also-be-removed"
